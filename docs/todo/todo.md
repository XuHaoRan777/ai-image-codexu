# Todo

## 已完成

- [x] 2026-06-08 调整生图失败处理：provider 或图片保存失败时后端硬删除任务记录，失败任务不再进入历史列表，前端轮询 404 时提示未保存历史。
- [x] 2026-06-08 历史页新增生图任务删除功能：列表项右侧添加删除按钮，后端提供 `DELETE /api/image-jobs/:id`，硬删除数据库记录并清理对应本地图片文件。
- [x] 2026-06-07 优化识图页左侧操作面板滚动条闪烁：滚动条默认透明，仅在 hover/focus 时显现，保留内容超高时的滚动能力。
- [x] 2026-06-07 识图页新增“元素提取”快捷提示词，用于提取图片视觉元素并输出可直接复制到生图模型的相似图片提示词。
- [x] 2026-06-07 优化识图页面细节：左右栏比例对齐生图页 3.5:6.5，强化“开始分析”禁用态，并将右侧分析结果从原始文本改为 Markdown 渲染。
- [x] 2026-06-07 新增无持久化识图功能：复用辅助模型配置，后端提供 `POST /api/image/recognize`，前端新增“识图”导航、上传分析页面和五个快捷提示词。
- [x] 2026-06-07 放大图片预览 Dialog 改为纯图片展示，移除“生成结果/历史图片”标题、描述区和内部滚动条，把空间分配给图片区域。
- [x] 2026-06-07 优化生图加载态与大图预览：移除冗余状态标签、标题和进度条，放大加载圈，提示词优化期间禁用生图/重试，并放大图片预览 Dialog。
- [x] 2026-06-07 优化辅助模型系统提示词，明确以用户原意为最高优先级，只在不偏离原意的前提下补充生图细节和负面约束。
- [x] 2026-06-07 将辅助模型请求地址字段从 `baseUrl` 改为 `url`，并改为按页面配置的完整 endpoint 直接请求，不再自动拼接 OpenAI/Claude 路径。
- [x] 2026-06-06 修复辅助模型启用 Switch 只改表单不落库的问题，改为切换即保存并同步左侧在线状态。
- [x] 2026-06-06 在配置页辅助模型表单补充启用 Switch，保存后可将左侧辅助模型状态切换为在线。
- [x] 2026-06-06 打通辅助模型提示词优化流程，前端优化成功后直接替换提示词文本域，不再显示单独优化结果卡。
- [x] 2026-06-06 优化生图体验：限制单任务生成、重做生成中预览态、放大任务预览和支持生成/历史图片点击放大及多图切换。
- [x] 2026-06-06 将生图任务记录改为 `image_job` 表持久化，新增后端历史列表接口，并通过 Vite `/api` 代理修复开发环境图片加载路径。
- [x] 2026-06-06 新增 AiCodeWith 生图来源，接入异步任务轮询，并按分辨率/数量自动选择 `gpt-image-2-beta` 或 `gpt-image-2` 请求参数。
- [x] 2026-06-06 将生图 provider 请求超时和前端任务轮询窗口延长至 5 分钟，并将 provider 错误日志改为多行格式化 JSON。
- [x] 2026-06-05 shared 新增 `ImageProviderTypeEnum`，后端 provider 分发改用枚举 case，并将各来源 base URL 内联到对应方法。
- [x] 2026-06-05 重构生图 provider 分发文件，移除 OpenAI-compatible 中间方法，让每个来源保留独立完整请求配置。
- [x] 2026-06-05 为后端生产源码中的 controller、service、provider、公共过滤器/拦截器和工具方法补充方法级注释。
- [x] 2026-06-05 新增 image-youyu 生图来源，后端接入固定文生图/图生图接口且请求体不发送 `model` 字段。
- [x] 2026-06-05 后端生图 provider 请求失败时仅打印第三方接口返回摘要，便于测试真实请求流程。
- [x] 2026-06-05 生图页面尺寸新增自适应选项，并接入 OpenAI/OneTopAI 与 Google provider 请求逻辑。
- [x] 2026-06-05 移除生图模型配置中的请求地址字段，运营商地址固定在后端 provider 方法内，并优化新增/编辑表单三行两列布局。
- [x] 2026-06-05 删除生图模型配置中的模型类型维度，仅保留来源类型，并将任务历史改为记录来源类型和实际请求模型名。
- [x] 2026-06-05 模型配置新增来源类型并接入 OpenAI、Google Gemini、OneTopAI 真实生图 provider，移除单独来源配置组件。
- [x] 2026-06-05 修复 Toast 动画不明显的问题，新增显式入场/退出状态和自定义 keyframes。
- [x] 2026-06-05 为前端增加全局动画过渡系统，覆盖页面切换、面板入场、列表项 stagger 和交互 hover。
- [x] 2026-06-05 修复历史页和配置页刷新后回到生图页的问题，主页面切换改为 hash 路由驱动。
- [x] 2026-06-05 优化配置页模型配置列表项样式，将启用 Switch 放入列表项，并新增后端模型启停专用接口。
- [x] 2026-06-04 修复 Radix Switch 状态样式，并将配置 API key 改为加密密文入库、掩码出接口。
- [x] 2026-06-04 修复配置页新增/编辑表单启用配置 Switch 和模型名输入宽度，并移除后端默认测试配置种子。
- [x] 2026-06-04 后端配置页：新增生图模型配置实体，并将 `image-model-configs` 接口接入 TypeORM Repository。
- [x] 2026-06-04 后端配置页：新增辅助模型配置实体，并将 `assistant-config` 接口接入 TypeORM Repository。
- [x] 2026-06-04 重做前端暗色创作控制台样式，覆盖生图、历史、模型配置中心，并完成生图页和配置页精简。
- [x] 2026-06-04 根据截图反馈继续收敛前端布局：移除页面标题区、历史详情与模型表单改为居中 Dialog、简化空状态和任务预览。
- [x] 2026-06-04 移除三个主页面顶部刷新按钮，仅保留有消息时的反馈提示。
- [x] 2026-06-04 统一三个主页面顶部与左右边缘的内容边距。
- [x] 2026-06-04 简化 `App.tsx`，将生图、历史、配置三个页面拆分到 `apps/web/src/pages`。
- [x] 2026-06-04 新增顶部全局 Toast 工具，支持成功、警告、错误、普通提示四种状态。
- [x] 2026-06-04 调整生图页创作与预览列宽，新增最多 6 张参考图上传，并将参数更新为尺寸、分辨率和数量。
- [x] 2026-06-04 将生图页创作与任务预览列宽调整为 4:6，并限制参数区一行最多 2 个组件。
- [x] 2026-06-04 区分优化提示词与创建生图任务的加载状态，优化时创建按钮只置灰禁用。
